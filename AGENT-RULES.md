# Agent Rules

## CRITICAL — START WITH A TOOL CALL
- Your VERY FIRST action must be a tool call (e.g., Bash: git pull). Do NOT generate ANY text before your first tool call. No preamble. No planning. Just call a tool immediately.

## Shared Rules
- Site capped at 10-12 sections. No free additions.
- MOBILE FIRST. Most users search for restaurants on phones.
- No desktop-only features (hover effects, cursor interactions).
- CENTER ALIGNMENT: All content must be consistently center-aligned on mobile. This is a core requirement.
- If unsure about a decision, text user via iMessage (chat_id 'any;-;+14847162152') AND add to QUESTIONS.md. Don't block.
- Only read the LAST 10 lines of CHANGELOG.md, not the whole file.
- Use targeted grep/read instead of reading entire files when possible.
- ALWAYS text user via iMessage after completing work.
- SIMPLICITY IS KING — Do NOT add animations, keyframes, hover transforms, glows, pulses, shimmers, bounces, or lifts. Only allowed: scroll reveal fade-in and menu/accordion transitions. Basic hover color/underline changes are fine.
- Nigel must PENALIZE over-animation. Too many moving elements = lower score.
- Spark: CAN add impressive new features, but if you add something, REPLACE or REMOVE something else. No piling on. Every addition should make the site simpler or better, not heavier.
